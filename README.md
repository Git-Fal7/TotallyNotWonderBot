# TotallyNotWonderBot
```
  Your very normal discord bot, logs excluded !
```

```
  To start clone the repostory, then execute this command :
  `npm install --save file-system@2.2.2 discord.js@11.5.0 canvas@2.6.1 array-sort@1.0.0 numeral@2.0.6`
  edit the botconfig in the commands folder
  and start your bot with `node index.js`
```
